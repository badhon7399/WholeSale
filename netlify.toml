[build]
  base = "frontend"
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

# Set up redirects for Next.js if necessary (handled automatically by Netlify's Next.js Essential Plugin)
